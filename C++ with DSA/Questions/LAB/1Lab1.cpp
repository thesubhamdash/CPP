#include<iostream>
using namespace std;

int main(){
    FILE *fp;
    fp=fopen("ASCII.txt", "r");
    int freq[256] = {0};
    char ch;

    while (!EOF)
    {
        // if ()
        // {
        //     freq[ch] += 1;
        // }
        
    }
    
    return 0;
}